import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.gerentefinanceiro',
  appName: 'gerentefinanceiro',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
