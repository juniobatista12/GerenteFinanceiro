import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.perfilaluno',
  appName: 'perfilaluno',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
